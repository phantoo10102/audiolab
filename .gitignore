# Configuration
config.yaml
data/config/.secret_key
data/config/user_settings.yaml

# Keep example config
!config.yaml.example

# Python cache
__pycache__/
*.pyc

# Runtime outputs (only if safe to ignore)
data/
data/log/
